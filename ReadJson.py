#This reads the file in json format
#Here it automatically allocates the layer,devices based on the file data
from Layer import Layer
from DeviceNode import DeviceNode
from Mobile import Mobile

class ReadJson():
    pass